import React, { Fragment } from 'react';

import { Form } from '../Form/Form';
import('./addItems.css');

const AddItem = () => {
  return (
    <Fragment>
      <Form />
    </Fragment>
  );
};

export default AddItem;
